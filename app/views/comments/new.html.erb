<div class="container is-desktop">
  <div class="content mx-5">
    <h1>New comment</h1>
    <%= turbo_frame_tag "new_comment", target: "_top" do %>
      <%= render 'form', post: @commentable, comment: @comment %>
    <% end %>
  </div>
</div>